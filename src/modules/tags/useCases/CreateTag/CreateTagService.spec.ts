import { CreateTagService } from "./CreateTagService";

describe('CreateTag', () => {
  it('should be able to create a new tag', () =>{
  });

  it('should not be able to create two tags with the same name', () =>{
  });
});